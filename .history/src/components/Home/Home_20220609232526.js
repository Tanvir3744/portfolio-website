import React from 'react';

const Home = () => {
    return (
        <div>
            this is home  
            <button className="btn btn-primary">primary button</button>
        </div>
    );
};

export default Home;